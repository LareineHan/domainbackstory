import{out,domainOf,fj}from"./_common.js";export async function onRequestGet({request}){const d=domainOf(request);if(!d)return out({error:"Invalid domain"},400);try{const p=new URLSearchParams({url:d+"/*",output:"json",fl:"timestamp,original,statuscode,mimetype",collapse:"urlkey",limit:"300"});p.append("filter","statuscode:200");p.append("filter","mimetype:text/html");const x=await fj("https://web.archive.org/cdx/search/cdx?"+p,30000);const rows=[];if(Array.isArray(x)&&x.length>1){const h=x[0];for(const v of x.slice(1)){const o=Object.fromEntries(h.map((k,i)=>[k,v[i]]));const t=o.timestamp||"";const date=t.length>=8?`${t.slice(0,4)}-${t.slice(4,6)}-${t.slice(6,8)}T00:00:00Z`:t;rows.push({date,url:o.original||"",archive_url:`https://web.archive.org/web/${t}/${o.original||""}`})}}rows.sort((a,b)=>String(a.date).localeCompare(String(b.date)));const idx=rows.length?[0,Math.floor(rows.length/2),rows.length-1]:[];return out({count:rows.length,first:rows[0]?.date||null,last:rows.at(-1)?.date||null,sample:[...new Set(idx)].map(i=>rows[i])})}catch(e){return out({error:"Wayback lookup failed: "+e.message},502)}}